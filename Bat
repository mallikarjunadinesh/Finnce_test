@echo off
setlocal

REM Go to the folder where this script is
cd /d "%~dp0"

REM Install required packages
python -m pip install --upgrade pip
python -m pip install -r requirements.txt

REM Run the app
python -m uvicorn app:app --host 127.0.0.1 --port 8000

pause
